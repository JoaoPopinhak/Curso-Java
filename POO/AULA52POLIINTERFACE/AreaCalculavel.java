package POO.AULA52POLIINTERFACE;

public interface AreaCalculavel {
	
	//Dentro da interface você só pode declarar metodos abstratos
	//todos os metodos de uma interface são abstratos essa é a princpal diferença entre as classes e interfaces
	
	
	double calculaArea();

}
